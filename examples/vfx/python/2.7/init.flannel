#!/usr/bin/env bash
# set up
load_modules "flannel/pather" "flannel/revdeps"
clean_closet "vfx/python" "2.7" "${@:(-1)}"

# pellets
remove_broken_revdeps "vfx/python" "${@:(-1)}"

# does
create_path "PYTHONPATH" "/2.7" "${@:(-1)}"
