#!/usr/bin/env bash
# set up
load_modules "fuzzy_plaid/pather" "fuzzy_plaid/pellets" "fuzzy_plaid/requires"
clean_closet "vfx/vray" "1.6.10" "${@:(-1)}"

# needs
load_requirement "vfx/maya" "==" "2012" "${@:(-1)}"

# pellets
define_revdep "vfx/maya" "==" "2012" "vfx/vray/1.6.10" "${@:(-1)}"

# does
create_path "VRAY_1_6_10" "/vray/1.6.10" "${@:(-1)}"
prepend_path "PYTHONPATH" "/vray/1.6.10" "${@:(-1)}"
