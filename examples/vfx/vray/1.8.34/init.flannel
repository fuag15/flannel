#!/usr/bin/env bash
# set up
build_closet "fuzzy_plaid/pather" "fuzzy_plaid/pellets" "fuzzy_plaid/requires"
clean_closet "vfx/vray" "1.8.34" "${@:(-1)}"

# needs
load_requirement "vfx/maya" "==" "2013" "${@:(-1)}"

# pellets
define_revdep "vfx/maya" "==" "2013" "vfx/vray/1.8.34" "${@:(-1)}"

# does
create_path "VRAY_1_8_34" "/vray/1.8.34" "${@:(-1)}"
prepend_path "PYTHONPATH" "/vray/1.8.34" "${@:(-1)}"
