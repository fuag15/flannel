#!/usr/bin/env bash
# set up
build_closet "pather" "pellets" "requires"
clean_closet "vfx/vray" "1.9.02" "${@:(-1)}"

# needs
requires_plaid "vfx/maya" ">=" "2013" "${@:(-1)}"

# pellets
pellet "vfx/maya" ">=" "2013" "vfx/vray/1.9.02" "${@:(-1)}"

# does
create_path "VRAY_1_9_02" "/vray/1.9.02" "${@:(-1)}"
prepend_path "PYTHONPATH" "/vray/1.9.02" "${@:(-1)}"
#!/usr/bin/env bash