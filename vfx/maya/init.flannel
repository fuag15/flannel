#!/usr/bin/env bash
if [[ "${@:(-1)}" == "clear" ]]; then
  echo "not clearing"
  flannel "vfx/maya/*" "$@"
elif [[ -n "$1" ]]; then
  echo "clearing maya version ${1}"
  flannel "vfx/maya/!(${1})" clear
else
  flannel vfx/maya/2013
fi