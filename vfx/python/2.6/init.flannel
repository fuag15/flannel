#!/usr/bin/env bash
# set up
build_closet "fuzzy_plaid/pather" "fuzzy_plaid/pellets"
clean_closet "vfx/python" "2.6" "${@:(-1)}"

# pellets
lint_pellets "vfx/python" "${@:(-1)}"

# does
create_path "PYTHONPATH" "/2.6" "${@:(-1)}"
