#!/usr/bin/env bash
# set up
build_closet pather pellets
flannel "vfx/python/!(2.6|*.flannel)" clear

# pellets
lint_pellets vfx/python/2.6 "${@:(-1)}"

# does
create_path PYTHONPATH /2.6 "${@:(-1)}"
