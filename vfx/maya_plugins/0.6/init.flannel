#!/usr/bin/env bash
# set up
build_closet "pather" "requires"

# clean up
clean_closet "vfx/maya_plugins" "0.6" "${@:(-1)}"

# needs
requires_plaid "vfx/maya" "==" "2013" "${@:(-1)}"

# does
append_path "MAYA_PLUGIN_PATH" "/plugin/0.6" "${@:(-1)}"
append_path "PYTHONPATH" "/plugin/0.6" "${@:(-1)}"